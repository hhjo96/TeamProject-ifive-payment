spring:
  application:
    name: commerce-payment-system
  cloud:
    aws:
      region:
        static: ap-northeast-2

  # 클라이언트 API 계약 설정 파일 import
  config:
    import: optional:classpath:client-api-config.yml

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8

  security:
    user:
      name: demo
      password: demo123

server:
  port: 8080
  servlet:
    context-path: /

# PortOne Configuration
portone:
  api:
    base-url: https://api.portone.io
    secret: ${PORTONE_API_SECRET:your-api-secret}
  webhook:
    secret: ${WEBHOOK_SECRET:your-api-secret}
  store:
    id: ${PORTONE_STORE_ID:your-store-id}
  channel:
    kg-inicis: ${PORTONE_CHANNEL_KG:your-kg-inicis-channel-key}
 #   toss: ${PORTONE_CHANNEL_TOSS:your-toss-channel-key}
# JWT Configuration
jwt:
  secret: ${JWT_SECRET:commercehub-secret-key-for-demo-please-change-this-in-production-environment}
  token-validity-in-seconds: ${JWT_VALIDITY:86400}  # 24 hours


# UI Branding Configuration
app:
  ui:
    branding:
      app-name: CommerceHub
      tagline: 통합 커머스 플랫폼
      logo-text: 🏪 CommerceHub
  add-test-products: true
  add-test-memberships: true


  # 고객의 포인트와 멤버쉽 정보를 업데이트 하는 시각
  # 기본적으로 새벽 3시
  schedules:
    point-membership-batching : "*/5 * * * * *"

  # 고객인 환불을 할 수 있는 기간
  # 기본적으로 7일
  business:
    refund-period: 10s

logging:
  level:
    com.spartaifive.commercepayment: DEBUG
    org.springframework.security: INFO

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: never
  info:
    env:
      enabled: true